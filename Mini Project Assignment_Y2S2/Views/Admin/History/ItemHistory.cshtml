@* @model IEnumerable<Mini_Project_Assignment_Y2S2.Models.History>

// needed item info
@{
    ViewData["Title"] = "History (Item)";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="mb-4">
    <h2 class="text-xl font-bold">History (Item)</h2>
    <p class="text-gray-400 text-sm">Lost / Found</p>
</div>

<div class="space-y-6">
    @if (Model != null && Model.Any())
    {
        @foreach (var item in Model)
        {
            // COLOR BASED ON STATUS
            // item 'Lost'(Red)
            // item 'Found' (Green)
            bool isLost = item.Status?.ToLower() == "lost";
            string bgClass = isLost ? "bg-red-100" : "bg-green-100";

            <div class="@bgClass rounded-lg p-6 flex gap-6 shadow-sm">

                <div class="w-32 h-32 bg-gray-200/50 rounded flex items-center justify-center shrink-0">
                    <i class="far fa-image text-3xl text-gray-400"></i>
                </div>

                <div class="flex-1">
                    <h3 class="font-bold text-lg mb-1">@item.ItemName</h3> <div class="text-sm text-gray-700 mb-4 whitespace-pre-line">
                        <span class="font-semibold">Description:</span>
                        @item.Description
                    </div>

                    <div class="text-sm text-gray-600 mb-4 space-y-1">
                        <p>
                            <span class="font-semibold">Date:</span> @item.Date.ToString("dd MMM yyyy") &nbsp;
                            <span class="font-semibold">Location:</span> @item.Location
                        </p>
                        <p>
                            Status: <span class="font-semibold">@item.Status</span>
                        </p>
                    </div>

                    <button class="bg-white/80 border border-gray-300 px-4 py-1 text-sm rounded hover:bg-white shadow-sm transition">
                        View Details
                    </button>
                </div>
            </div>
        }
    }
    else
    {
        <div class="text-center text-gray-500 py-10">
            <i class="fas fa-inbox text-4xl mb-3"></i>
            <p>No history records found.</p>
        </div>
    }
</div>

<div class="mt-6 flex justify-center gap-4 text-sm text-gray-500">
    <span class="cursor-pointer hover:text-gray-800">&larr; Previous</span>
    <span class="bg-gray-800 text-white px-2 rounded">1</span>
    <span class="cursor-pointer hover:text-gray-800">2</span>
    <span class="cursor-pointer hover:text-gray-800">3</span>
    <span>...</span>
    <span class="cursor-pointer hover:text-gray-800">Next &rarr;</span>
</div> *@


